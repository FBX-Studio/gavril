// Курированные ответы для подготовки к экзамену.
// Можно перегенерировать вопросы/ссылки из файлов, но ответы останутся стабильными.

export const ANSWERS: Record<number, string> = {
  1: `Растровая графика — изображение как сетка пикселей.

Ключевые понятия:
- Пиксель: минимальный элемент, хранит цвет (и иногда альфу).
- Разрешение: $W\times H$ (например, 1920×1080).
- PPI/DPI: плотность точек при печати (ppi — «пикселей на дюйм» как характеристика изображения).

Плюсы/минусы:
- Плюс: отлично для фото и плавных градиентов.
- Минус: при увеличении появляется пикселизация (теряется резкость на границах).`,

  2: `Параметры растра:
- Разрешение ($W\times H$): влияет на детализацию.
- Глубина цвета: число бит на пиксель/канал (8 бит/канал → 256 уровней; 24 бит RGB → ~16.7 млн цветов).
- Тоновый (динамический) диапазон: сколько различимых уровней яркости/деталей в тенях и светах.

Пример:
Фото 4000×3000, 24 bpp выглядит детальнее, чем 800×600, 8 bpp; при узком диапазоне «проваливаются» тени/пересветы.`,

  3: `Форматы графических файлов (что важно на экзамене):
- BMP: обычно без сжатия → большой размер, простая структура.
- PNG: без потерь, альфа‑канал, хорош для интерфейса/скриншотов.
- JPEG: с потерями (DCT + квантование) → хорош для фото, плохо для текста/резких границ.
- GIF: палитра до 256 цветов, поддержка простой анимации.
- TIFF: контейнер для полиграфии/архива, может быть и без потерь, и сжатым, поддерживает CMYK и большую глубину.

Критерии выбора: «фото vs графика», «нужна ли прозрачность», «важен ли размер».`,

  4: `Векторная графика — изображение как набор геометрических примитивов (линии, кривые Безье, многоугольники).

Ключевые понятия:
- Примитивы: отрезок, окружность, эллипс, кривая Безье.
- Контур (path): набор сегментов/кривых.
- Узлы (anchor points) и управляющие точки кривых.

Плюсы/минусы:
- Плюс: масштабирование без потерь, удобно для логотипов/иконок/схем.
- Минус: плохо для фотореализма и текстур (нужна растризация).`,

  5: `Сравнение растра и вектора:

Растр:
- + Фото/градиенты/текстуры.
- − Пикселизация при увеличении, зависит от разрешения.

Вектор:
- + Масштабирование без потерь, малый размер для простых сцен.
- − Для вывода на экран всё равно растризуется; сложные сцены могут быть тяжёлыми.

Типичные примеры:
- Фото — растр (JPEG/PNG).
- Логотип/схема — вектор (SVG/PDF).`,

  6: `Восприятие света человеком:
- Глаз воспринимает яркость нелинейно (примерно логарифмически: закон Вебера–Фехнера).
- Есть палочки (чувствительность, яркость, сумерки) и колбочки (цвет, дневное зрение).
- Спектральная чувствительность выше в зелёной области → отсюда веса luma.

Практический вывод в КГ:
- Гамма‑коррекция: нельзя интерполировать/усреднять «гамма‑закодированные» значения как линейные.
- Яркость (luma) часто оценивают $Y\approx 0.299R+0.587G+0.114B$ (для sRGB‑подобных).`,

  7: `Кривые реакции глаза:
- Палочки: максимум чувствительности смещён, не различают цвет.
- Колбочки: 3 типа (условно L/M/S) → чувствительность к длинноволновому/среднему/короткому диапазону.

Свойства зрения:
- Нелинейность по яркости (различаем относительные изменения).
- Чувствительность к яркости выше, чем к хроме.
- Высокая «важность» зелёного канала (и в сенсорах, и в кодеках).`,

  8: `Классификация ПО для графики:
- Растровые редакторы (Photoshop, GIMP) — пиксели.
- Векторные редакторы (Illustrator, Inkscape) — кривые/примитивы.
- 3D‑пакеты (Blender, Maya, 3ds Max) — сцены/рендер/анимация.
- CAD/САПР (AutoCAD, SolidWorks) — точная геометрия/чертежи.
- Композитинг/видео (After Effects, Nuke) — слои/эффекты.

Критерий: какой тип данных и для каких задач.`,

  9: `Краткая история КГ (вехи):
- 1960‑е: Sketchpad (Сазерленд) — интерактивная графика.
- 1970‑80‑е: растровые дисплеи, GUI, первые стандарты.
- 1990‑е: 3D‑ускорители, OpenGL/DirectX, массовая 3D‑графика.
- 2000‑е+: программируемые шейдеры, GPU‑рендеринг, PBR.
- 2010‑е+: real‑time трассировка лучей, VR/AR.

Идея: рост вычислений → от линий к фотореализму и интерактивности.`,

  10: `Характеристики цвета:
- Тон (Hue): «какой цвет» на круге 0–360°.
- Насыщенность (Saturation): степень отличия от серого.
- Светлота/яркость (Lightness/Value): насколько светлый/тёмный.

Зачем в КГ:
- Удобные регулировки (подбор цвета, коррекция, UI‑пипетки).
- Разделение яркости и хромы помогает обработке и сжатию.`,

  11: `Аддитивные vs субтрактивные модели:
- Аддитивная (RGB): складываем свет; (0,0,0) — чёрный, максимум — белый.
- Субтрактивная (CMY/CMYK): краски поглощают свет; базовое — белая бумага.

Где применяется:
- RGB: экраны, камеры, Web.
- CMYK: печать (добавляют K для «настоящего» чёрного и экономии).`,

  12: `RGB:
- Цвет как три компоненты R,G,B.
- Обычно 8 бит/канал: 0..255.
- Смешение — аддитивное (свет).

Важно:
- Для физически корректных расчётов сначала переводят в линейное пространство (gamma decode), потом операции, потом обратно.`,

  13: `CMY:
- Субтрактивная модель: C, M, Y «вычитают» компоненты белого света.
- Идеально: C=1−R, M=1−G, Y=1−B (если R,G,B нормированы 0..1).
- На практике для печати используют CMYK.`,

  14: `CMYK:
- C, M, Y + K (black).
- K нужен, потому что смесь CMY в реальной печати даёт грязный тёмный, а не глубокий чёрный.

Упрощённо (для понимания):
- Перевод из RGB обычно делают через промежуточные формулы/профили ICC.
- На экзамене: главное различие «свет vs краска» и роль канала K.`,

  15: `HSV:
- H (тон): угол 0–360°.
- S (насыщенность): 0..1.
- V (яркость/value): 0..1.

Где удобно:
- Пипетки/палитры: человеку проще выбирать «тон + насыщенность + яркость», чем R,G,B.`,

  16: `Однотонная (flat) закраска:
- Для полигона считается один цвет (обычно по нормали полигона и свету).
- Внутри полигона цвет не меняется.

Плюсы/минусы:
- + Очень быстро.
- − Видны грани, «многогранность».`,

  17: `Метод Гуро (Gouraud):
- Считаем освещение в вершинах.
- Затем интерполируем цвета/интенсивности по ребрам и по скан‑линиям.

Плюсы/минусы:
- + Быстрее Фонга.
- + Гладче, чем flat.
- − Спекулярный блик может «пропасть» или размазаться (если пик яркости не попал в вершину).`,

  18: `Метод Фонга (Phong shading):
- Интерполируются нормали (или векторы), а освещение считается в каждом пикселе.
- Даёт более правильные блики.

Типовая модель освещения (смысл):
- Фоновая + диффузная (Ламберт) + зеркальная (Phong/Blinn).
- Диффузная часть зависит от $\max(0,\langle N,L\rangle)$.
- Зеркальная зависит от степени $n$ (чем больше n, тем «острее» блик).`,

  19: `Заливка по границе (boundary fill):
- Заливка области, ограниченной контуром заданного цвета.

Алгоритм:
- Стартуем из точки внутри.
- Расширяемся на соседей (4‑ или 8‑связность), пока не встретили цвет границы.

Проблемы:
- «Дыры» в границе → утечка заливки.
- Рекурсия может переполнить стек → используют очередь/стек итеративно.`,

  20: `Алгоритм Брезенхэма для отрезка:
- Быстрая растеризация линии целочисленной арифметикой.
- Идём по ведущей оси (по x или по y) и ведём накопленную ошибку, выбирая следующий пиксель.

Суть:
- Не используем float.
- Детерминированный набор пикселей.

Псевдо‑идея:
- $err = dx - dy$; на каждом шаге корректируем err и x/y.`,

  21: `Растеризация окружности (midpoint circle / Bresenham circle):
- Используем симметрию окружности: достаточно вычислить 1/8 дуги.
- Решающее выражение (ошибка) позволяет выбирать ближайший пиксель между двумя кандидатами.

Плюсы:
- Целочисленные вычисления.
- Быстро и симметрично.`,

  22: `Сазерленд–Ходгман (отсечение многоугольника выпуклым окном):
- Последовательно «режем» многоугольник каждой стороной окна.
- На каждом шаге просматриваем ребра многоугольника (S→E) и формируем новый список вершин.

4 случая (для точки относительно границы):
- inside→inside: добавить E.
- inside→outside: добавить пересечение.
- outside→inside: добавить пересечение и E.
- outside→outside: ничего.

Важно:
- Корректен для выпуклого окна.
- Результат — новый многоугольник (может иметь больше вершин).`,

  23: `Заполнение многоугольника (scanline fill):
- Проходим строками (y), на каждой строке ищем пересечения с ребрами.
- Сортируем x‑координаты пересечений, закрашиваем попарно (между 1‑2, 3‑4, ...).

Пространственная когерентность:
- Соседние пиксели/строки похожи → пересечения меняются «плавно».
- Можно обновлять пересечения инкрементально (Active Edge Table), а не пересчитывать заново.`,

  24: `Сглаживание ступенчатости: supersampling (увеличение частоты выборки):
- Рендерим в более высоком разрешении (например, 2× по каждой оси).
- Затем усредняем/фильтруем до целевого разрешения.

Плюсы/минусы:
- + Универсально и просто.
- − Дорого: 2× по оси → 4× пикселей, больше вычислений/памяти.`,

  25: `Сглаживание: полутоновые методы (dithering):
- Имитация промежуточных уровней яркости/цвета за счёт узоров из доступных цветов.

Варианты:
- Упорядоченный дизеринг (матрица порогов, Bayer).
- Ошибка‑диффузия (Floyd–Steinberg): распределяем ошибку квантования на соседей.

Где полезно:
- Ограниченная палитра/битность (старые дисплеи, печать, стилизация).`,

  26: `Яркость и контраст (обработка):
- Яркость: добавление смещения: $I' = I + b$.
- Контраст: масштабирование относительно середины: $I' = (I-\mu)\cdot c + \mu$ (часто берут \mu=128 для 8‑бит).

Графики (передаточная функция):
- Яркость — сдвиг по вертикали.
- Контраст — изменение наклона.

Важно:
- После операции нужен clamp в диапазон [0,255].`,

  27: `Масштабирование (resampling):
Проблемы:
- При увеличении: размытие/ступеньки.
- При уменьшении: алиасинг (мерцание/моар).

Методы интерполяции:
- Nearest (ближайший сосед): быстро, но «квадраты».
- Bilinear: мягче, но может размывать.
- Bicubic/Lanczos: лучше качество, дороже.

Правило:
- Перед уменьшением нужен фильтр низких частот (anti‑alias), иначе появятся артефакты.`,

  28: `Поворот (2D):
Матрица поворота на угол $\theta$:
$$
\begin{pmatrix}x'\\y'\end{pmatrix}=
\begin{pmatrix}\cos\theta & -\sin\theta\\ \sin\theta & \cos\theta\end{pmatrix}
\begin{pmatrix}x\\y\end{pmatrix}
$$

Поворот вокруг точки (cx,cy):
- Переносим в начало: (x−cx, y−cy)
- Поворачиваем
- Возвращаем: + (cx,cy).`,

  29: `Цифровые фильтры (свёртка):
- Фильтр задаётся ядром (матрицей коэффициентов).
- Выходной пиксель — сумма произведений соседей на коэффициенты.

Характеристики:
- Линейность/инвариантность к сдвигу (для свёртки).
- Нормировка (сумма коэффициентов для сохранения яркости).

Примеры 3×3:
- Размытие (box): все 1, нормировка 1/9.
- Резкость (пример):
  [0 -1 0; -1 5 -1; 0 -1 0].`,

  30: `2D‑преобразования: перенос по X
- $x' = x + d_x$; $y' = y$.

В однородных координатах:
$$
\begin{pmatrix}x'\\y'\\1\end{pmatrix}=
\begin{pmatrix}1&0&d_x\\0&1&0\\0&0&1\end{pmatrix}
\begin{pmatrix}x\\y\\1\end{pmatrix}
$$`,

  31: `2D‑преобразования: перенос по Y
- $x' = x$; $y' = y + d_y$.

Однородная матрица:
$$
\begin{pmatrix}1&0&0\\0&1&d_y\\0&0&1\end{pmatrix}
$$`,

  32: `2D‑преобразования: отображения (отражения)
- Относительно оси X: (x, y) → (x, −y)
- Относительно оси Y: (x, y) → (−x, y)
- Относительно начала: (x, y) → (−x, −y)

Матрицы — частный случай масштабирования с коэффициентами −1.`,

  33: `2D‑преобразования: повороты
- Используется матрица поворота (см. вопрос про поворот).
- Композиция преобразований — умножение матриц (важен порядок!).

Правило порядка:
- Если применяем сначала A, потом B, то итоговая матрица $M = B\cdot A$.`,

  34: `Преобразование единичного квадрата:
- Аффинное преобразование переводит квадрат в параллелограмм.
- Матрица 2×2 задаёт, куда переходят базисные векторы (1,0) и (0,1).
- Перенос задаёт, куда переходит (0,0).

Идея:
- Столбцы матрицы — новые направления осей (масштаб+поворот+сдвиг/скос).`,

  35: `Принципы сжатия изображений:
- Удаление избыточности: статистической (энтропийное кодирование) и психовизуальной.
- Без потерь: RLE, Huffman, LZ‑семейство (PNG).
- С потерями: преобразование → квантование → кодирование (JPEG).

JPEG (порядок):
- RGB→YCbCr, часто subsampling Cb/Cr.
- DCT 8×8.
- Квантование (главный источник потерь).
- Zig‑zag, RLE, Huffman/arith.`,

  36: `Цифровая стеганография:
- Скрытое внедрение сообщения в «контейнер» (изображение/аудио/видео), чтобы факт передачи был незаметен.

Роль в защите:
- Скрытые метки, водяные знаки (авторские права), скрытые каналы.

Отличие от криптографии:
- Криптография скрывает смысл.
- Стеганография скрывает сам факт передачи.`,

  37: `Скрытие данных в неподвижных изображениях (общая идея):
- Встраивание в пространственной области (пиксели/LSB) или частотной (DCT/вейвлеты).

Критерии качества:
- Незаметность (перцептуальная).
- Ёмкость (сколько бит можно спрятать).
- Устойчивость (к сжатию, фильтрам, обрезке).`,

  38: `Методы скрытия данных в неподвижных изображениях:
- LSB (пространственная область): простая, высокая ёмкость, низкая устойчивость.
- Частотные методы (DCT/DFT/DWT): лучше устойчивость (например, к JPEG), сложнее.
- Метод замещения, метод распределения спектра, QIM‑подобные подходы.

На экзамене: сравнить по трём критериям: незаметность/ёмкость/устойчивость.`,

  39: `LSB (least significant bit):
- Меняем младшие биты пикселей (например, в канале B) на биты сообщения.

Плюсы/минусы:
- + Очень просто, высокая ёмкость.
- − Плохо переживает JPEG/фильтры/шум, сравнительно легко детектируется статистикой.

Ёмкость (пример):
- 1 бит на пиксель в RGB‑изображении размера W×H → W·H бит (если 1 канал).`,

  40: `ЦВЗ (цифровой водяной знак):
Назначение:
- Защита авторских прав, идентификация источника, контроль целостности.

Требования:
- Незаметность.
- Устойчивость (или наоборот хрупкость — для контроля изменений).
- Возможность надёжного обнаружения/извлечения.`,

  41: `Типы ЦВЗ по устойчивости:
- Робастные (устойчивые): сохраняются после сжатия/фильтрации/масштабирования.
- Хрупкие (fragile): разрушаются при малейшем изменении → контроль целостности.
- Полухрупкие (semi‑fragile): терпят «допустимые» операции (например, JPEG), но ломаются при подделке.`,

  42: `Понятие «стего»:
- Стего‑сообщение (stego message): контейнер после внедрения.
- Стего‑ключ: секретный параметр, управляющий внедрением/извлечением.
- Стего‑канал: скрытый канал передачи.`,

  43: `Стегодетектор:
Назначение:
- Определить, есть ли скрытая информация.
- Иногда — оценить вероятность, локализацию или тип метода.

Подходы:
- Статистические тесты (аномалии распределений).
- ML‑классификация по признакам.`,

  44: `Общая структура стегосистемы:
- Вход: контейнер + сообщение + ключ.
- Встраиватель → стего‑контейнер.
- Канал передачи/искажения.
- Извлекатель (с ключом) → сообщение.
- Детектор (опционально) → факт наличия/оценка.`,

  45: `Устройства обнаружения и выделения ЦВЗ:
- Детектор: проверяет наличие водяного знака (да/нет, метрика уверенности).
- Экстрактор: пытается восстановить биты/идентификатор.

Используют:
- Секретный ключ/псевдослучайные последовательности.
- Корреляционные методы, пороговые решения.`,

  46: `Классификация систем встраивания ЦВЗ:
- По области: пространственная (пиксели), частотная (DCT/DWT/DFT).
- По типу: добавление (additive), замещение (replacement), квантование (QIM).
- По видимости: видимые/невидимые.
- По устойчивости: робастные/хрупкие/полухрупкие.`,

  47: `Контейнер: выбранный/случайный/навязанный
- Выбранный: атакующий может выбрать контейнер из множества (worst‑case).
- Случайный: контейнер случайно выбран из распределения.
- Навязанный: атакующий задаёт контейнер (наиболее сильная модель угроз).`,

  48: `Контейнер: потоковый и фиксированный
- Потоковый: данные приходят как поток (видео/аудио/стрим), встраивание «на лету».
- Фиксированный: заранее известный файл/изображение.

Различие:
- В потоковом важны задержки и устойчивость к потерям/сжатиям.
- В фиксированном проще оптимизировать размещение.`,

  49: `Требования к стегосистеме и ЦВЗ:
- Незаметность (перцептуальная и статистическая).
- Устойчивость к типичным преобразованиям (для робастных ЦВЗ).
- Достаточная ёмкость (для стего‑сообщений).
- Безопасность по ключу (без ключа извлечь/удалить сложно).
- Надёжность детектирования (низкие FP/FN).`,

  50: `Математическая модель стегосистемы:
- Контейнер $C$, сообщение $M$, ключ $K$.
- Встраивание: $S = E(C, M, K)$ (stego‑контейнер).
- Извлечение: $\hat{M} = D(S, K)$.

Требования:
- $D(E(C,M,K),K)=M$ (в идеале) или с малой ошибкой.
- Незаметность: $S$ статистически близок к $C$.
- Устойчивость: $D(T(S),K)\approx M$ при допустимых искажениях $T$.`,

  51: `Классы стегосистем по доступной доп.информации (классические модели):
- Stego‑only: есть только стего‑контейнер.
- Known‑cover: есть исходный контейнер и стего.
- Known‑message: известно встроенное сообщение.
- Chosen‑message: можно выбирать сообщения для встраивания.
- Chosen‑stego/cover: можно выбирать контейнеры/наблюдать результат (сильнее).`,

  52: `Способы внедрения битов ЦВЗ:
- Замещение: прямое подменивание (например, LSB).
- Добавление: $S = C + \alpha W$ (добавляем слабый сигнал/шум по маске).
- Квантование: QIM (кодируем бит выбором квантовочного интервала).
- Модуляция в частотной области: изменение выбранных коэффициентов DCT/DWT.

Выбор зависит от нужной устойчивости и незаметности.`,

  53: `Линейное встраивание (additive watermarking):
- Типовая формула: $S = C + \alpha\cdot W$.
  Где $C$ — контейнер (или коэффициенты), $W$ — псевдослучайный знак/последовательность, $\alpha$ — сила.

Обнаружение:
- Корреляция: сравниваем $S$ (или его признаки) с $W$.
- Решение по порогу (если корреляция выше порога → знак присутствует).

Баланс:
- Больший $\alpha$ → выше обнаружение, но хуже незаметность.`,

  54: `Алгоритм Kutter (пространственная область):
Идея (упрощённо):
- Встраивание в яркостную компоненту с учётом чувствительности зрения.
- Часто используют псевдослучайный знак и добавляют его пропорционально локальной яркости/текстуре.

Цель:
- Увеличить устойчивость по сравнению с простым LSB, сохраняя незаметность.

На экзамене: подчеркнуть «перцептуальную маскировку» и добавочную модель $C+\alpha W$.`,

  55: `Алгоритм Bruyndonckx (пространственная область):
Идея (общее описание):
- Робастное встраивание водяного знака с использованием корреляции и маскирования.
- Выбираются области/пиксели, где изменения менее заметны, добавляется слабый сигнал.

Ключевые слова:
- Псевдослучайная последовательность.
- Корреляционный детектор.
- Маскировка по текстуре/яркости.`,

  56: `Алгоритм Langelaar (пространственная область):
Идея (общее описание):
- Встраивание в домене изображения с ориентацией на практическую устойчивость.
- Используются локальные блоки/маски и детектирование по корреляции.

На экзамене: выделить принципы: «встраивание слабого сигнала», «ключ», «корреляция», «маскировка».`,

  57: `Алгоритм Pitas (пространственная область):
Идея (общее описание):
- Встраивание/детектирование водяного знака с опорой на статистические свойства изображения.
- Часто формулируется как добавление/модуляция сигнала в выбранных пикселях/областях.

Как отвечать:
- Указать модель $S=C+\alpha W$ (или модуляцию), наличие ключа и корреляционный детектор.
- Обсудить компромисс: незаметность ↔ устойчивость.
`,
}
